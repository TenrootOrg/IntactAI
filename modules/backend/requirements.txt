# MSSP Backend - Core Dependencies (always installed)
# Module-specific dependencies are in requirements-{module}.txt

# Web Framework
Flask==3.1.2
flask-cors==6.0.2
Werkzeug==3.1.6  # Pin - 3.1.7 breaks internal Docker webhooks with strict host validation

# HTTP & Data
requests==2.32.5
PyYAML==6.0.3

# Scheduler
APScheduler==3.11.2
SQLAlchemy==2.0.46
